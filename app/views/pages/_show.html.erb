<div class="container-fluid modal-margin">
  <div class="py-3 d-flex align-items-center justify-content-between">
    <h3 class="m-0"><strong><%= project.name %></strong></h3>
    <%= link_to project.url, class: 'project-modal-name', target: 'blank' do %>
      <p class="m-0">Visit www project</p>
    <% end %>
  </div>

  <hr>

  <div class="row my-3">
    <div class="col-12 modal-image">
      <% if project.photo_url.present? %>
        <%= image_tag project.photo_url %>
      <% end %>
    </div>
    <div class="col-12 mt-5">
      <div class="proj-desc-color">
        <p class=""><%= project.description %></p>
      </div>
      <div class="project-students mb-2">
        <% project.students.each do |s| %>
          <div class="">
            <%= image_tag s.avatar_url, class: 's-avatar-bordered'  %>
            <p class=""><strong><%= s.name.split[0].capitalize %></strong></p>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <hr>
      <div class="d-flex justify-content-between align-items-center ">
        <div>
          <button type="button" class="btn btn-light" data-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>

</div>
